# This is a Databricks asset bundle definition for dab_EvenLakeFlow.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: poc_pl_lhcl

include:
  - resources/*.yml
  - resources/*/*.yml
  - variables/*.yml


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-2ca8ad08-8da1.cloud.databricks.com
      root_path: /Workspace/Users/nmtc9to5@gmail.com/${var.bundle_instance_name}/.bundle/${bundle.name}/${bundle.target}
      state_path: /Workspace/Users/nmtc9to5@gmail.com/${var.bundle_instance_name}/.bundle/${bundle.name}/${bundle.target}/state
    
  prod:
    mode: production
    workspace:
      host: https://dbc-2ca8ad08-8da1.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nmtc9to5@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nmtc9to5@gmail.com/.bundle/${bundle.name}/${bundle.target}